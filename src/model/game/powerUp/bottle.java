package model.game.powerUp;

import model.game.data.Position;
import model.game.data.Size;

public class bottle extends Power{
    public bottle(Size size, Position pos, String img, String type) {
        super(size, pos, img, type);
    }
}
